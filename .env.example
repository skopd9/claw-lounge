# Required — house agents
ANTHROPIC_API_KEY=sk-ant-...
ASI_ONE_API_KEY=...

# Optional — web search citations
YOU_COM_API_KEY=...

# Optional — shared room across browsers.
# Without these, the lounge runs in solo mode (per-tab conversation in localStorage).
# Get these from Vercel KV or Upstash Redis REST.
KV_REST_API_URL=
KV_REST_API_TOKEN=
